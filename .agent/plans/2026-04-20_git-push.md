# Git Push Workflow: esp32s3-cam & JS Improvements

`esp32s3-cam` の安定性向上と、フロントエンドでのカメラ接続状態の同期に関する変更をコミットし、リモートリポジトリ（masterブランチ）にプッシュします。

## User Review Required

> [!IMPORTANT]
> **js/config.js の扱いについて**
> `js/config.js` に Gemini API キーが含まれています。このファイルは `.gitignore` に含まれていますが、過去にコミットされた履歴があるため `git status` に表示されています。
> セキュリティ保護のため、今回はこのファイルを **コミット対象から除外** します。
> また、今後このファイルが追跡されないよう `git rm --cached js/config.js` を実行することを推奨します。

## Proposed Changes

### esp32s3-cam

#### [MODIFY] [esp32s3-cam.ino](file:///c:/Users/kouga/Projects/Web/toio-lab-web/esp32s3-cam/esp32s3-cam.ino)
- SSID を `Pixel_7_Pro` に更新。
- 画像データ送信時に `client.write` を使用するように修正（PSRAMバッファへの対応）。
- WiFi の自動再接続設定（`setAutoReconnect(true)`）と、`loop()` 内での5秒ごとの再接続チェックを追加。

#### [NEW] [README.md](file:///c:/Users/kouga/Projects/Web/toio-lab-web/esp32s3-cam/README.md)
- esp32s3-cam のセットアップと使用方法に関するドキュメント。

### Frontend

#### [MODIFY] [app.js](file:///c:/Users/kouga/Projects/Web/toio-lab-web/js/app.js)
- アプリ起動時および設定画面でのカメラURL更新時に、カメラの接続状態をチェックしてUIを更新するロジックを追加。

## Open Questions

- `js/config.js` を `git rm --cached` して追跡対象から外してもよろしいでしょうか？（ローカルファイルは残ります）

## Verification Plan

### Automated Tests
- `git status` でステージングされたファイルを確認。

### Manual Verification
- コミット後に `git log` でメッセージを確認。
- `git push` の成功を確認。
